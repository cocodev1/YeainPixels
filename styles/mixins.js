import { StyleSheet } from 'react-native'

const mainStyle = StyleSheet.create({
    bigContainer: {
        left: 0,
        right: 0,
        alignItems: 'center',
    },
    emotion: {
    }
})

export default mainStyle